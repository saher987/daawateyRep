<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Android App Links: invitation links (SMS/email, the ones
                 InviteeRow.jsx builds as `${origin}/i/<token>`) open directly
                 in the app instead of the browser, when installed. Requires
                 autoVerify (Android checks assetlinks.json — see
                 frontend/public/.well-known/assetlinks.json — before it will
                 route here instead of the browser) and BROWSABLE so it can
                 be reached from a tapped link, not just launcher icon. The
                 host below must exactly match APP_URL (see DEPLOYMENT.md) —
                 update both together if the prod domain ever changes.

                 Was "www.daawatey.com" — real invitation links use
                 window.location.origin (InviteeRow.jsx), which in prod is
                 the bare "daawatey.com" (no www, confirmed against the
                 actual deployed site), so this intent-filter never matched
                 a single real link. www kept alongside as a second <data>
                 in case anything ever does resolve there.

                 /my-invitations added too: InvitationPage.jsx's "more
                 details" button navigates there (not just /i/<token>) once
                 signed in, and Android honors App Links for that kind of
                 same-tab navigation (unlike iOS Universal Links, which need
                 an actual tap on a link element) — so this is what lets
                 that button hand off to the app too, not just the original
                 invitation link. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="daawatey.com"
                    android:pathPrefix="/i/" />
                <data
                    android:scheme="https"
                    android:host="daawatey.com"
                    android:path="/my-invitations" />
                <data
                    android:scheme="https"
                    android:host="www.daawatey.com"
                    android:pathPrefix="/i/" />
                <data
                    android:scheme="https"
                    android:host="www.daawatey.com"
                    android:path="/my-invitations" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
